    - requirements.txt contains all the modules you will be needing to run the django application. 
    So you first create a virtual envt, activate it and then in the virtual envt., install all the modules you will need. 
    Generally do "pip install -r requirements.txt"

    - To run the website on a local server, do "python manage.py runserver" and open in the browser.